app = "musicbot-rfsieg"
primary_region = "fra"

[build]

[processes]
  app = "python bot.py"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  auto_stop_machines = false
  auto_start_machines = false
